import React from "react"

import Page from "../Page"

// import acousticPanelImage from "./assets/acoustic-panel.jpg"
// import styles from './index.css'

const About = (props) => (
  <Page { ...props }>
    {/* <div
      style={{ backgroundImage: `url(${acousticsImage})`}}
      className={ styles.content }
    >
    </div> */}
  </Page>
)

export default About
